scalar DraftText

extend type Query {
  # FIXME: should remove after redux removed
  getDraftText(input: String): DraftText
}

extend type SettingObjectType {
  # FIXME: should remove after new type created
  shopperLoginMessageDraft: DraftText
}
