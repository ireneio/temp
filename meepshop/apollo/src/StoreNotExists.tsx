// import
import React from 'react';

import styles from './styles/storeNotExists.less';

// definition
export default React.memo(() => (
  <div className={styles.root}>... 無此商店 / The store does not exist ...</div>
));
