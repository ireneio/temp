enum ProductCollectionsModuleType {
  ORIGIN
  SIDE
}

type ProductCollectionsModule implements Module {
  id: ID!
  parentId: ID!
  productCollectionsType: ProductCollectionsModuleType!
  percentWidth: PercentWidth!
  product(productId: ID): Product
}