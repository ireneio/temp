extend type User {
  groupClient: MemberGroup
  tel: String
  mobile: String
  country: String
  city: String
  county: String
  street: String
}

extend type MemberGroup {
  expireDate: Timestamp!
  startDate: Timestamp!
  unlimitedDate: Boolean!
}
