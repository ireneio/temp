export const root = {
  flexGrow: 0,
};

export const button = colors => ({
  height: '50px',
  minWidth: '150px',
  color: colors[2],
  borderColor: colors[4],
  backgroundColor: colors[4],
  borderRadius: '0px',
});
