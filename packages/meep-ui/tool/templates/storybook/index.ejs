import { storiesOf } from '@storybook/react';

export default () => {
  require('tool/storybook/stories/context');

  // stories
<% stories.forEach(storyName => { -%>
  require('<%= storyName %>/__story__/.cache');
<% }) -%>
};
