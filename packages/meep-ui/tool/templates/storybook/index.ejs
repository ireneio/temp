import { storiesOf } from '@storybook/react';

export default () => {
  require('storybook/stories/context');

  // stories
<% stories.forEach(storyName => { -%>
  require('<%= storyName %>/__story__/.cache');
<% }) -%>
};
