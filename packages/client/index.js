const gql = require('graphql-tag');
const {
  useQuery,
  useMutation,
  useApolloClient,
} = require('@apollo/react-hooks');

module.exports = {
  gql,
  useQuery,
  useMutation,
  useApolloClient,
};
