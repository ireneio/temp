# apiVersion: v1
# kind: Service
# metadata:
#   labels:
#     app: next-store
#   name: next-store
# spec:
#   ports:
#     - port: 14402
#       targetPort: 14402
#       nodePort: 31100
#   selector:
#     app: next-store
#   type: NodePort
apiVersion: v1
kind: Service
metadata:
  labels:
    app: next-store
  name: next-store
spec:
  type: LoadBalancer
  ports:
    - port: 14401
      name: http-port
      targetPort: 14401
  selector:
    app: next-store