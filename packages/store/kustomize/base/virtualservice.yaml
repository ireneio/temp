apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: next-store-virtualservice
  namespace: default
spec:
  http:
  - route:
    - destination:
        host: next-store
        port:
          number: 14401
