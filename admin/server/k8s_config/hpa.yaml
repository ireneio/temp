apiVersion: extensions/v1beta1
kind: HorizontalPodAutoscaler
metadata:
  name: next-admin
  namespace: default
spec:
  scaleRef:
    kind: Deployment
    name: next-admin
    subresource: scale
  minReplicas: 1
  maxReplicas: 10
  cpuUtilization:
    targetPercentage: 50
