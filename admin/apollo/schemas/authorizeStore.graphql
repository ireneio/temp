input AuthorizeStoreInput {
  token: String!
}

enum AuthorizeStoreEnum {
  OK
  AUTHORIZE_STORE_FAIL
}

type AuthorizeStoreResponse {
  status: AuthorizeStoreEnum!
}

extend type Mutation {
  authorizeStore(input: AuthorizeStoreInput!): AuthorizeStoreResponse!
}
